---
name: ai-detection-probe
description: "Use when auditing Arabic text for AI-detection risk."
version: 3.0.0
author: عبدالله العجيان (falconeye90)
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [arabic, ai-detection, turnitin, academic-integrity, text-audit, humanizer, quantitative, publishing]
    category: productivity
    related_skills: [arabic-humanizer, text-cleaning-agent, arabic-academic-documents]
    publication: public
    repo: https://github.com/falconeye90/hermes-skills
---
# ai-detection-probe — فاحص نصوص لاكتشاف AI

**Bilingual — Arabic 🇸🇦 & English 🇬🇧.** فاحص كمّي محلي يقدّر «كم يقرأ النص كذكاء
اصطناعي»، من إشارات رخيصة **قابلة للشرح سطراً بسطر** (تُطبع مع النتيجة، فتعرف *لماذا*
جاء التقدير كما جاء). اللغة تُكتشف تلقائياً.

> ⚠️ أكاديمياً/أخلاقياً: هذه الأداة للدراسة الذاتية والتحسين والشفافية، وليست بديلاً
> عن فحص النزاهة الرسمي. الإثبات النهائي يبقى لنظام مؤسستك، والكتابة بمساعدة
> الذكاء الاصطناعي تتطلب التصريح وفق سياسات جامعتك.

> 🚫 **ليست محاكاة لـTurnitin.** توثيق Turnitin الرسمي يصف نموذجه بأنه «أنماط
> تعلّمية كثيرة تعمل معاً، لا مجموعة صغيرة من **قواعد شفافة يقرأها الإنسان**»
> — أي أن لا سكربت محلي يعيد إنتاج حكمه. هذه إشارات **مستوحاة من المقاربة
> الإحصائية الشائعة** (burstiness / أنماط، لا perplexity بنموذج حقيقي)، وعتباتها
> **اجتهادية غير معايَرة** على أي كاشف.

---

## التثبيت / Install

```bash
pip install python-docx      # إن أدخلت .docx
pip install pypdf            # إن أدخلت .pdf
```

## الاستخدام / Usage

```bash
python3 scripts/turnitin_sim.py document.docx      # عربي أو إنجليزي (تلقائي)
python3 scripts/turnitin_sim.py notes.txt --json   # مخرجات للأنابيب/الأتمتة
python3 scripts/turnitin_sim.py                    # عرض تجريبي مدمج
```

---

## نموذج التسجيل (v2) — ثلاثة مؤشرات بأوزان صريحة

| المؤشر | الوزن | AUC منفردة | بشري | AI |
|--------|:-----:|:----------:|------|-----|
| **Lexical — root-TTR (Guiraud)** مقلوبة، تُحتسب عند ≥300 كلمة فقط | 0.30 | **0.867** | G مرتفع | G منخفض |
| **Burstiness** — تباين أطوال الجمل (يحتاج ≥ 6 جمل) | 0.25 | 0.845 | `CV ≥ 0.45` | `CV < 0.30` |
| **AI patterns** — عبارات مُرجَّحة بالخطورة (`scripts/ai_patterns.py`: قاطع 2.0/مشتبه 1.0) | 0.30 | 0.598 | صفر إشارات | يتشبّع عند 3 قاطعة |
| **Human markers** — جمل قصيرة جداً · أرقام/وحدات · أفعال ميدانية («شغّلنا/قست/لاحظت») · تنوّع بدايات الجمل | 0.15 | 0.765 | 3/3 | 0/3 |
| **TTR الخام** | **يُعرض ولا يُحتسب** | — | — | — |

**النتيجة النهائية** = تقدير AI-likeness + نطاق حكم **مقيس**:
`≥ 35%` **AI-leaning** (يلتقط 82.8% من الآلي و6.5% من البشري) ·
`20–35%` **غير حاسم — منطقة التداخل المقيسة** · `< 20%` **human-leaning** (61.3% من البشري و3.4% من الآلي).

> 📐 **المعايرة (2026-09-23):** AUC = **0.965** على 29 نصاً آلياً (4 موديلات)
> مقابل 31 بشرياً (ويكيبيديا العربية، 300–600 كلمة) · أفضل عتبة 25 (J=0.836 · دقة 91.7%).
> الأمر: `python3 calibration/calibrate.py {fetch-human,make-ai,measure}` — التفاصيل
> في `calibration/README.md`. **قبل المعايرة** كان التركيب 0.55/0.30/0.15 يعطي AUC 0.898.

### لماذا TTR لا يُحتسب؟ (درس مُقيس)
في v1 كانت المؤشرات **أربعة بأوزان متساوية**، وTTR على النصوص القصيرة يقارب 1.0
للبشري والآلي معاً — فلا يميّز. والنتيجة أن **نصاً فيه 7 عبارات AI قاطعة ومسودة
ميدانية بشرية حصلا على نفس الدرجة `2.0/4`**: المؤشر الذي يميّز فعلاً كان يساوي
**نقطة واحدة من أربع**. الآن TTR **معلوماتي فقط**، والإشارات القاطعة تقود.

### الانحدار الشريف
أي إشارة **لا يمكن حسابها بصدق** لهذا المدخل (أقل من 6 جمل، محتوى فارغ) **تُسقَط
ويُعاد توزيع وزنها** — ولا تُحتسب صفراً. والنصوص تحت **200 كلمة** تُصدر تحذيراً
صريحاً: التقدير **تلميح لا نتيجة**.

---

## العتبات الموثّقة

الجدول الكامل **بمصدر كل عتبة** (مقيسة/تقريبية) وببديل TTR للنصوص القصيرة
(root-TTR/Guiraud · MTLD) في **`references/thresholds.md`**. القاعدة: أي عتبة بلا
مصدر رسمي تُكتب **«تقريبي»** صراحةً — لا تُزعم دقة غير موجودة.

**بديل TTR للنصوص القصيرة:** إذا كان `W < 300` فاحسب `G = V/√N` (Guiraud 1954)
واعرضه بجانب TTR، ولا تُصدر حكماً حاسماً على الفئة المعجمية وحدها. الأداة تفعل ذلك
تلقائياً وتُظهر `root-TTR (Guiraud)` في القسم [4].

---

## قاموس الأنماط (مساهمة معلم)

في `scripts/ai_patterns.py`: **87 نمطاً** (44 عربي · 43 إنجليزي) موزّعة على **7 فئات**:

| الفئة | المعنى |
|-------|--------|
| `connectives` | روابط جاهزة (بالإضافة إلى ذلك، furthermore) |
| `openers` | صيغ استهلال الجمل/الفقرات |
| `overstating-importance` | تضخيم الأهمية (محوري/جوهري/pivotal) |
| `vague-attribution` | إسناد غامض (يرى الخبراء/it is noted) |
| `negative-parallelism` | توازي سلبي (ليس فقط… بل / not only… but also) |
| `promotion` | لغة ترويج/حماس (استثنائي/groundbreaking) |
| `punctuation-markers` | علامات ترقيم (شرطات مكرّرة، فواصل منقوطة متجاورة) |

كل نمط بوزن: **قاطع `2.0`** (blocker) أو **مشتبه `1.0`** (suspect) — والترجيح النهائي
**جمع الأوزان لا عدّ الأنماط** (يتشبّع عند 3 أنماط قاطعة). والقاموس يدعم نوعين:
`phrase` (مطابقة كلمة/عبارة كاملة) و`regex` (نمط ترقيمي/بنيوي).

---

## قراءة النتيجة

```bash
python3 -m unittest discover -s tests -v     # 16 عقداً سلوكياً
```
في `tests/fixtures/` زوج مقصود (نص مشبع بـAI / مسودة بشرية) بكل لغة، والاختبارات
تشترط أن **يفصل بينهما ≥ 40 نقطة** ويقعا في نطاقين متعاكسين. شغّله قبل أن تثق
بأي تعديل على العتبات.

---

## منهجية التحسين (متى كان الخطر مرتفعاً)

1. **كسر التماثل:** نوّع طول الجمل (قصيرة/متوسطة/طويلة) عمداً في كل فقرة.
2. **استبدال أنماط AI:** أزِل كل نمط في القائمة وبدِّله بصياغة بشرية.
3. **تخصيص المفردات:** استبدل العام المتكرر بمصطلح ميداني دقيق.
4. **أضف بِصمتك:** أرقامك، اعتراضاتك، ما فشل في تجربتك — أقوى إشارة بشرية.

---

## حدود الأداة (اقرأها قبل الاستخدام)

- **تقدير ذاتي لا كاشف**، وغير معايَر على Turnitin أو غيره؛ والعتبات اجتهادية.
- النصوص **القصيرة** (< 200 كلمة أو < 6 جمل) غير مستقرة — والأداة تقول ذلك في
  تحذيراتها بدل أن تخمّن.
- العناوين وقوائم المراجع تُخفّف إحصاء الجمل.
- لا شيء هنا يضمن نتيجة، ولا يصح استخدامه **لتنكّر authorship** — الإفصاح عن
  استخدام أدوات المراجعة اللغوية هو الدفاع الذي يصمد فعلاً.

---

## مثال حقيقي

```
AI-likeness estimate: 95%  ->  AI-leaning

[1] AI PATTERNS  ... x 'بالإضافة إلى ذلك': 3 (strong) | weighted total: 12.0
[2] BURSTINESS   mean len: 16.9 | CV: 0.277 -> too uniform (AI-like)
[3] HUMAN MARKERS  very short sentences: 0 | digits: 0 | field verbs: 0 | markers: 1/3
[4] LEXICAL      TTR: 0.726 — informational only
! only 186 words — treat it as a hint, not a result
```

---

## البنية

```
ai-detection-probe/
├── SKILL.md                  # هذا الملف
├── README.md
├── references/
│   └── thresholds.md         # جدول العتبات بمصدر كل عتبة (مساهمة معلم) + الأرقام المقيسة
├── calibration/
│   ├── calibrate.py          # أدوات المعايرة: fetch-human · make-ai · measure
│   ├── README.md             # المنهجية + النتيجة + حدود القياس
│   └── results.json          # الملخّص المقيس (الدليل) — الكوربوس نفسه لا يُرفع
├── scripts/
│   ├── turnitin_sim.py       # المحرّك: قراءة المدخلات + الإشارات + التقرير
│   └── ai_patterns.py        # 87 نمطاً (عربي/إنجليزي) في 7 فئات بأوزان (مساهمة معلم)
└── tests/
    ├── fixtures/             # زوج عيّنات (AI / بشري) بكل لغة
    └── test_scoring.py       # 16 عقداً سلوكياً
```

## الرخصة

MIT — عبدالله العجيان (falconeye90). استخدم بحرية، ونسب عند الاقتباس.
